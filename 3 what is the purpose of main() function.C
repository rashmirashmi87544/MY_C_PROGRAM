#include <stdio.h>

int main() {
    
    printf("the main() function is the stating point of the every c program.\n");
    printf("it tells the compiler where the program begins execution.\n");
    printf("without main the program cannot run.\n");

    return 0;
}
